# Local Claude Code config — contains GITHUB_TOKEN and machine-local settings.
# Never commit; the token lives in the Claude Project instructions and in the
# gitignored .claude/settings.local.json on this machine.
.claude/

# SimC HTML raid report (36 MB) — regeneration artifact for
# simc-guides/build-spell-ids-reference.js (curl'd to repo root per its USAGE
# header). Never commit; regenerate on demand. See DECISIONS.md.
_simc_raid.html

# Raw WoW combat logs (~14 GB) — local development resource for spell ID
# discovery and parser testing. Never commit; too large and personal.
Logfiles/

# Parser output — derived from Logfiles/, regenerate with: node parse-logs.js
spell-id-report.txt
spell-id-report.json

# OS / editor cruft
Thumbs.db
.DS_Store
